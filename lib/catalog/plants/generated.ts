/**
 * Pipeline-added wild edible plants. Empty for now — the research pipeline that
 * extends the mushroom catalog can populate this file the same way, and it will
 * merge into PLANT_CATALOG via lib/plant-catalog.ts without touching the curated
 * seed set.
 */
import type { PlantSpecies } from "../../plant-types";

export const GENERATED_PLANTS: PlantSpecies[] = [];
